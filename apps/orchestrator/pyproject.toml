[tool.poetry]
name = "orchestrator"
version = "0.1.0"
description = "FastAPI Orchestrator for PRism AI"
authors = ["PRism AI Team"]

[tool.poetry.dependencies]
python = "^3.10"
fastapi = "^0.111.0"
uvicorn = "^0.30.0"
pydantic = "^2.7.0"
httpx = "^0.27.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
